class Post {
  int post_id;
  String title;
  int user_id;
  int like_count;
  int support_count;
  String post_photo_url;
  String link;

  Post(this.post_id, this.title, this.user_id, this.like_count,
      this.support_count, this.post_photo_url, this.link);
}
