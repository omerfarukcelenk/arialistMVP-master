import 'package:arialist/models/User.dart';

class Connect {
  User first_user_id;
  User second_user_id;
  int is_connected;

  Connect(this.first_user_id, this.second_user_id, this.is_connected);
}
